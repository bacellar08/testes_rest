cors:
  originsPatterns: http://localhost:8080
server:
  port: 8888
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    open-in-view: true
springdoc:
  paths-to-match: /api/**
  swagger-ui:
    use-root-path: true

